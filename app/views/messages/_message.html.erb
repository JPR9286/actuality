<div id="message-<%= message.id %>" data-mon-controller-target='messageContent' data-mon-controller-id="<%= message.id %>">
  <small>
    <strong><%= message.user.nickname %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
